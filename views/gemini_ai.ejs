<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Gemini AI Movie Search</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background: #f2f2f2;
      padding: 40px;
    }
    h1 {
      text-align: center;
      color: #333;
    }
    form {
      display: flex;
      justify-content: center;
      margin-bottom: 30px;
    }
    input[type="text"] {
      padding: 10px;
      font-size: 1rem;
      width: 60%;
      border: 2px solid #ccc;
      border-radius: 8px 0 0 8px;
    }
    button {
      padding: 10px 20px;
      background: #4361ee;
      color: white;
      font-weight: bold;
      border: none;
      border-radius: 0 8px 8px 0;
      cursor: pointer;
    }
    ul {
      background: white;
      max-width: 800px;
      margin: 0 auto;
      border-radius: 10px;
      padding: 20px;
      box-shadow: 0 5px 15px rgba(0,0,0,0.1);
    }
    li {
      margin-bottom: 10px;
      color: #444;
    }
  </style>
</head>
<body>
  <h1>🎬 Gemini AI Movie Search</h1>
  <form method="GET" action="/gemini">
    <input type="text" name="q" value="<%= prompt %>" placeholder="Ask something like: 'Best action movies after 2015'">
    <button type="submit">Search</button>
  </form>

  <% if (movies.length > 0) { %>
    <ul>
      <% movies.forEach(line => { %>
        <li><%= line %></li>
      <% }) %>
    </ul>
  <% } else if (prompt) { %>
    <p style="text-align:center; color: gray;">No results found.</p>
  <% } %>
</body>
</html>
